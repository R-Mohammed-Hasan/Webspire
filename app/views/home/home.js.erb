$posts = $('<%= j render(@posts) %>')
$('.postsContainer').append($posts);
<% if @posts.next_page %>
    $('.pagination').replaceWith('<%= j will_paginate(@posts) %>');
<% else %>
   $('.pagination').remove();
<% end %>
